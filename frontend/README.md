# Digital Studio Portfolio

This is the frontend app for a modern digital studio portfolio website built with React, Vite, TypeScript, Tailwind CSS v4, React Router, and Motion.

## Commands

```bash
npm install
npm run dev
npm run build
npx tsc --noEmit
npm audit
```

For the full project documentation, see `../README.md`.
